[project]
name = "gamesage"
version = "0.1.0"
description = "Open-source AI gaming companion."
requires-python = ">=3.12,<3.13"
dependencies = [
    "mss>=10.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.0",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]